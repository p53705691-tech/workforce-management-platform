# Core application dependencies
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Login==0.6.3
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4
python-dotenv==1.2.3
argon2-cffi==25.1.0
gunicorn==26.1.0
# Shared Flask-Limiter storage backend for production (see
# app.config.ProductionConfig / RATELIMIT_STORAGE_URI) — required once
# Gunicorn runs more than one worker, so per-IP rate limits stay
# accurate across all of them instead of resetting per worker.
redis==5.2.1
# PDF report rendering (app.services.pdf_reports). Pure-Python, no
# system-level libraries required (unlike an HTML/CSS-to-PDF renderer),
# which keeps the Docker image simple. Pulls in Pillow as its own
# dependency (prebuilt wheels, no system libs needed either).
reportlab==5.0.1

# Development / test dependencies
pytest==9.1.1
pytest-cov==7.1.0
freezegun==1.5.5
